## 目的

AchievementMasterの各種リソース(wood,gold,sheep)に、リソース毎に固有の採取時間を設定する。

## 仕様

- AchievementMasterの各種リソース(wood,gold,sheep)は、リソース毎に固有の採取時間をパラメータとしてもつ。例えば、woodは5秒、goldは10秒。
- プレイヤーがリソースに対してインタラクト(採取)するとき、対象の採取時間だけインタラクトし続けることで、採取完了する。
- 実績解除やスキル、装備により、リソースの採取時間を短縮できるようにしておく。
